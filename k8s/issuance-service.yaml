apiVersion: v1
kind: Service
metadata:
  name: issuance-service
  namespace: kube-credential
  labels:
    app: issuance-service
    component: backend
    service: issuance
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3001
    protocol: TCP
    name: http
  selector:
    app: issuance-service
